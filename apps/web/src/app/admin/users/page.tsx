import { AdminUsersClient } from './client'

/** Les données viennent de /api/admin/users (formule effective, usage, échéances). */
export default function AdminUsersPage() {
  return <AdminUsersClient />
}
